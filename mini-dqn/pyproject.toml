[project]
name = "mini-dqn"
version = "0.1.0"
description = "Value-based RL from scratch: tabular Q-learning to Double DQN"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium[classic-control]>=1.3.0",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "torch>=2.13.0",
]
